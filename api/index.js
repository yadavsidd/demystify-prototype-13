// This file acts as the entry point for the Vercel serverless function.
// It imports the configured Express app from the main server file and exports it.

import app from '../server/server.js';

export default app;
